node_modules/
dist/
.env
.env.local
.env.*.local
# Airtable → Aurora migration artifacts (live data — never commit)
db/export/
services/wellbound-api/registry.json
# AWS credentials — NEVER commit
*accessKeys*.csv
*credentials*.csv
"S3 BUCKET.txt"
S3 BUCKET.txt
*.md
# Logship / on-prem DB connection notes (never commit)
dbconfig.md
tools/hchb-dup-agent/.env
tools/hchb-dup-agent/.env.local
tools/hchb-dup-bridge/.deploy-secrets.env
# Clearinghouse / eligibility credentials (Waystar, Availity, Optum) — NEVER commit
services/wellbound-api/.deploy-secrets*.env
services/wellbound-api/.env
services/wellbound-api/.env.*
tools/hchb-dup-bridge/.aws-sam/
tools/hchb-dup-bridge/packaged.yaml
tools/hchb-dup-bridge/build/
.DS_Store
.vite/
*.log
coverage/
