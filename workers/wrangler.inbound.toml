name = "wellbound-inbound-email"
main = "worker-inbound-email.js"
compatibility_date = "2024-09-23"

# Deploy:
#   cd careStream/workers && npx wrangler deploy -c wrangler.inbound.toml
# Secrets:
#   npx wrangler secret put RESEND_API_KEY -c wrangler.inbound.toml
#   npx wrangler secret put RESEND_WEBHOOK_SECRET -c wrangler.inbound.toml
#   npx wrangler secret put INTERNAL_API_KEY -c wrangler.inbound.toml

[vars]
API_URL = "https://wzfb4i68yh.execute-api.us-east-2.amazonaws.com"
FILES_API_URL = "https://wzfb4i68yh.execute-api.us-east-2.amazonaws.com/files"
# Match any address on the Resend receiving subdomain (MX only on this host).
INBOUND_TO_ADDRESS = "inbound.wellboundcarestream.com"
